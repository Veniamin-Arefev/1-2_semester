#include <stdio.h>
#include <stdlib.h>
#include <math.h>

long long a[8][8];
int main(int argc, char** args) {
	char in[128];
	fgets(in, 128, stdin);
//	printf("%s", in);
//	printf("%d %d", in[0], in[1]);
	printf("Шамиль - пид**ас");
	printf("\n");
	return 0;
}

