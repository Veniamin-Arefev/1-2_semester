#include <stdio.h>

struct list {
    int x;
    struct list *big;
    struct list *next;
};

struct list removee(struct list *l, int x){
    struct list *prev;
    if (l->x == x){

    }
}

main (void) {

}